<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.postgres.sample.service.impl.dao.kjo.Kjo_CheckListDAO">
<!--
    <select id="cntByFacilityCode" parameterType="checkList" resultType="checkList">
        select count(*) as total from check_report where facility_code = #{facility_code}
    </select>
-->
    <select id="cntChkListByFacilityCode" parameterType="checkList" resultType="checkList">
        select count(*) as total from check_list where facility_code = #{facility_code}
    </select>

    <insert id="inputChkList" parameterType="checkList" >
        insert into check_list
        values(#{doc_no},#{facility_code},#{check_category},#{check_item},#{check_grade},#{note},#{checklistist_no})
    </insert>
</mapper>