<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.postgres.sample.service.impl.dao.kjo.Kjo_CheckItemInfoDAO">
<!--
    <select id="cntByFacilityCode" parameterType="checkList" resultType="checkList">
        select count(*) as total from check_report where facility_code = #{facility_code}
    </select>
-->

    <select id="findAllCheckItemInfo" resultType="checkItemInfo" >
        select * from check_item_info
    </select>
</mapper>